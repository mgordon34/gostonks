# Disable RDB snapshots because market queues are ephemeral and high write
# volume was blocking clients during background saves.
save ""

# Keep appendonly persistence off for maximum throughput.
appendonly no

# Avoid blocking writers if an unexpected persistence failure happens.
stop-writes-on-bgsave-error no
